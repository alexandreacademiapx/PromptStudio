{
  "name": "prompt-studio-iphone-app",
  "version": "1.0.0",
  "private": true,
  "description": "Empacotamento iPhone do Prompt Studio em Capacitor.",
  "scripts": {
    "copy": "npx cap copy",
    "sync": "npx cap sync",
    "open:ios": "npx cap open ios",
    "doctor": "npx cap doctor",
    "security:check": "node scripts/security-check.cjs && npm audit --audit-level=high"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.2",
    "@capacitor/ios": "^7.4.2"
  }
}
